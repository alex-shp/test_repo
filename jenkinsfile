pipeline {
    agent any

    stages {
        stage('Uload the file from Jen to S3') {
            steps {
                s3Upload bucket: 'test45890', file:'test1', workingDir: '/home/ec2-user/' 
            }
        }
    }
}