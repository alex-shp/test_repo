pipeline {
    agent any

    stages {
        stage('Uload the file from Jen to S3') {
            steps {
                s3Upload bucket: 'test45890', file:'test_upload', workingDir: '/home/ec2-user/test_upload' 
            }
        }
    }
}